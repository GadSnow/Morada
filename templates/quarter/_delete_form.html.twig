<form method="post" action="{{ path('app_quarter_delete', {'id': quarter.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce quartier?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quarter.id) }}">
	<button class="btn btn-danger mt-2">Supprimer</button>
</form>
